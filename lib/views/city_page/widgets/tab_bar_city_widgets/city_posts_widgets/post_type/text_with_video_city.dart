import 'package:flutter/material.dart';

class TextWithVideo extends StatelessWidget {
  const TextWithVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
