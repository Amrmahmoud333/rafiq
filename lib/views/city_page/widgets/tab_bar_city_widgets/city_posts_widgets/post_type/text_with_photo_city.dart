import 'package:flutter/material.dart';

class TextWithPhoto extends StatelessWidget {
  const TextWithPhoto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
